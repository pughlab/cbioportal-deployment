---
default_portal_settings:
  'db.user': "{{mysql_database_user}}"
  'db.password': "{{mysql_database_password}}"
  'db.host': "{{mysql_database_host}}"
  'app.name': "cBioPortal"
  'db.connection_string': "jdbc:mysql://{{mysql_database_host}}:3306/{{mysql_database_name}}"
  'mycancergenome.show': "false"

portal_settings: {}

## NOT SECURE -- "admin" / "pass" with bcrypt
## SHOULD ALWAYS OVERRIDE

## We use the password twice here, once encrypted (this is pushed into the server) and
## once clear, which is never stored but used to authenticate and write to the server.
oidc_admin_username: 'admin'
oidc_admin_password: 'pass'
oidc_admin_password_bcrypt: '$2a$05$JMcWp/w9v7jCrNI6CqAfOu6BgCH0eX6/X1KAqm8bmHodddlTV9Te6'
